<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        WindowStartupLocation="CenterScreen"
        x:Name="AppWindow"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:core="clr-namespace:Telefonico.Core;assembly=Telefonico.Core"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <local:ColumnDefinitionExtended Width="2*" />
            <local:ColumnDefinitionExtended Width="5*" />
        </Grid.ColumnDefinitions>
        
        
        <Frame Grid.Column="1" x:Name="MainFrame" Content="{Binding ApplicationViewModel.CurrentPage, Source={x:Static local:ViewModelLocator.Instance}, ConverterParameter=ApplicationViewModel, Converter={local:ApplicationPageValueConverter}}" RenderTransformOrigin="0.56,0.537" />
        
        <!-- Side Menu -->
        <local:SideMenuControl local:AnimateSlideInFromLeftProperty.Value="True" >
            <local:ChatListControl />
        </local:SideMenuControl>


    </Grid>
</Window>
