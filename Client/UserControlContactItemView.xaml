<UserControl x:Class="Client.UserControlContactItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="250">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--<StackPanel Grid.Column="0" Margin="5" VerticalAlignment="Center">
            <Label FontSize="18" Content="{Binding Name, FallbackValue=Mustermann}" Height="30"/>
        </StackPanel>-->

        <Button  Content="Mustermann" Foreground="Black" Background="Transparent" Grid.Column="0" Margin="5" VerticalAlignment="Center">
            
        </Button>
        
    </Grid>

</UserControl>
