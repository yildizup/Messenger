CREATE DATABASE telefonico;

create table user (
 
  id int not null AUTO_INCREMENT,
  email varchar(255),
  password varchar(255),
  
  primary key (id)
  
  );
  
  create table contacts (
  
  
  
  s
  ) ;